# 电源快捷按钮

双击 `电源快捷按钮.app` 即可使用，提供以下操作：

- 一键黑屏（只关闭显示器，移动鼠标或按键即可恢复）
- 锁定屏幕
- 进入睡眠
- 重新启动（二次确认）
- 关机（二次确认）
- 右键“关机”可设置或取消定时关机，并显示剩余倒计时

首次使用“重启”“关机”或“定时关机”时，macOS 可能请求系统控制或管理员权限。

如需重新构建，在终端进入本文件夹后运行：

```bash
./build.sh
```
